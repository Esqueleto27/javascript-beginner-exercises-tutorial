# Creación de HTML Básico con JavaScript

Este ejercicio practica la concatenación de strings para generar una estructura básica de HTML utilizando JavaScript.

El objetivo es combinar varias constantes que representan etiquetas HTML y construir un documento completo, asignándolo a la variable `htmlDocument`.

Finalmente, el resultado se imprime en la consola mostrando la estructura básica de un documento HTML.
